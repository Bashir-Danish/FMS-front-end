<script>

</script>
<template>
    <div>
      enrolls
    </div>
</template>
<style>
</style>









<!-- 

// Importing reactive from vue
import { computed, ref, watch, reactive } from "vue";

...

// Convert items array to a reactive object
const itemsReactive = reactive({
  list: items
});

...

// Modify handleItemClick to update the reactive object
const handleItemClick = (item: MenuItem) => {
  if (item.children && item.children.length > 0) {
    const foundItem = itemsReactive.list.find(i => i.name === item.name);
    if (foundItem) foundItem.open = !foundItem.open;
  } else {
    navigate(item.link);
  }
  console.log(item);
};
 -->
